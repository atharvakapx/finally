---
phase: 01-backend-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - backend/app/main.py
  - backend/app/routers/portfolio.py
  - backend/app/routers/watchlist.py
  - backend/app/routers/chat.py
  - backend/tests/test_main_integration.py
autonomous: true
requirements:
  - CORE-02
  - CORE-05
tags:
  - fastapi
  - market-data
  - sse
  - walking-skeleton
must_haves:
  truths:
    - "Lifespan startup constructs a PriceCache, calls create_market_data_source(cache), and awaits source.start(DEFAULT_TICKERS)"
    - "PriceCache and MarketDataSource are stored on app.state and accessible from the running app"
    - "Within 3 seconds of startup the PriceCache holds prices for all 10 default tickers"
    - "GET /api/stream/prices is reachable and returns a text/event-stream response that emits a price event within 3 seconds"
    - "Lifespan shutdown awaits source.stop() so the asyncio task is cancelled cleanly"
    - "Stub routers exist for portfolio, watchlist, chat; each registered endpoint returns 501 with {\"error\": \"not_implemented\", \"message\": \"...\"}"
    - "MASSIVE_API_KEY absent => SimulatorDataSource is used (verified by factory behavior)"
  artifacts:
    - path: "backend/app/main.py"
      provides: "Full lifespan: load_dotenv, init_db, PriceCache, market source, stub router registration, stream router, static mount"
      contains: "create_market_data_source"
      contains_also: "create_stream_router"
    - path: "backend/app/routers/portfolio.py"
      provides: "create_portfolio_router factory returning 501 stubs for GET /portfolio, POST /portfolio/trade, GET /portfolio/history"
      contains: "create_portfolio_router"
    - path: "backend/app/routers/watchlist.py"
      provides: "create_watchlist_router factory returning 501 stubs for GET, POST, DELETE /watchlist/{ticker}"
      contains: "create_watchlist_router"
    - path: "backend/app/routers/chat.py"
      provides: "create_chat_router factory returning 501 stubs for POST /chat"
      contains: "create_chat_router"
    - path: "backend/tests/test_main_integration.py"
      provides: "Lifespan happy-path test covering market source startup, stream endpoint, stub routes"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/market/factory.py"
      via: "create_market_data_source(cache) call inside lifespan"
      pattern: "create_market_data_source\\("
    - from: "backend/app/main.py"
      to: "backend/app/market/stream.py"
      via: "app.include_router(create_stream_router(cache), prefix=\"/api\")"
      pattern: "create_stream_router"
    - from: "backend/app/main.py"
      to: "backend/app/routers/{portfolio,watchlist,chat}.py"
      via: "app.include_router(create_<x>_router(...), prefix=\"/api\")"
      pattern: "create_(portfolio|watchlist|chat)_router"
---

<objective>
Complete the Phase 1 vertical slice by wiring the market data subsystem and scaffolding the remaining router structure. After this plan: the FastAPI app boots, the PriceCache is populated by the GBM simulator within seconds, `GET /api/stream/prices` streams live updates, and stub endpoints exist at `GET /api/portfolio`, `POST /api/portfolio/trade`, `GET /api/portfolio/history`, `GET /api/watchlist`, `POST /api/watchlist`, `DELETE /api/watchlist/{ticker}`, and `POST /api/chat` — each returning 501 with the documented error shape. Phases 2 and 3 will replace stub bodies without changing signatures or registration order.

Purpose: Deliver the third and final vertical slice of Phase 1. After Plan 03 the walking skeleton is complete: developer runs the server and observes a live SSE stream + a discoverable API surface. CORE-02 (market source on startup/shutdown) and CORE-05 (env var driven behavior — `MASSIVE_API_KEY` selects simulator vs. Massive) are satisfied here.
Output: Full `main.py` wiring, 3 stub routers, integration test asserting the entire happy path.
</objective>

<execution_context>
@/home/agent/finally/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-backend-foundation/01-CONTEXT.md
@.planning/phases/01-backend-foundation/01-PATTERNS.md
@.planning/phases/01-backend-foundation/01-SKELETON.md
@.planning/phases/01-backend-foundation/01-01-SUMMARY.md
@.planning/phases/01-backend-foundation/01-02-SUMMARY.md
@backend/CLAUDE.md
@backend/app/market/__init__.py
@backend/app/market/factory.py
@backend/app/market/stream.py
@backend/app/market/cache.py
@backend/app/market/interface.py
@backend/market_data_demo.py
@planning/PLAN.md

<interfaces>
<!-- From backend/app/market/__init__.py — public exports, do not modify. -->
- PriceCache()                                              # constructor; no args
  .update(ticker, price, timestamp=None) -> PriceUpdate
  .get_all() -> dict[str, PriceUpdate]
  .version                                                  # monotonic counter
- create_market_data_source(cache: PriceCache) -> MarketDataSource
- MarketDataSource (abstract)
  .start(tickers: list[str] | tuple[str, ...]) -> Awaitable[None]
  .stop() -> Awaitable[None]
  .add_ticker(ticker: str) -> Awaitable[None]
  .remove_ticker(ticker: str) -> Awaitable[None]
- create_stream_router(price_cache: PriceCache) -> APIRouter   # exposes GET /api/stream/prices (router itself has prefix /api/stream)

<!-- From app.db (created in Plan 02). -->
- init_db() -> None
- DEFAULT_TICKERS: tuple[str, ...]                         # 10 default tickers

<!-- Existing FastAPI primitives used here. -->
- FastAPI.include_router(router, prefix="")
- FastAPI.mount(path, app, name=None)
- StaticFiles(directory, html=False)
- JSONResponse(content, status_code=200)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create stub routers for portfolio, watchlist, chat</name>
  <files>backend/app/routers/portfolio.py, backend/app/routers/watchlist.py, backend/app/routers/chat.py</files>
  <read_first>
    - backend/app/market/stream.py (router factory pattern — closure over injected dep, prefix on the inner APIRouter)
    - .planning/phases/01-backend-foundation/01-PATTERNS.md (portfolio.py / watchlist.py / chat.py sections — factory + stub patterns are spelled out)
    - .planning/phases/01-backend-foundation/01-CONTEXT.md (D-04 scaffold rule, D-06 no module-level globals)
    - planning/PLAN.md §8 (API Endpoints — exact paths and HTTP methods to stub)
  </read_first>
  <behavior>
    - `backend/app/routers/portfolio.py` exports `create_portfolio_router(price_cache: PriceCache) -> APIRouter` (signature matches PATTERNS.md). The returned router has `prefix="/portfolio"` and `tags=["portfolio"]`. It registers three endpoints: `GET ""` (i.e. `/portfolio` after include_router with `/api`), `POST "/trade"`, `GET "/history"`. Each returns `JSONResponse({"error": "not_implemented", "message": "Coming in Phase 2"}, status_code=501)`.
    - `backend/app/routers/watchlist.py` exports `create_watchlist_router(price_cache: PriceCache) -> APIRouter`. Router has `prefix="/watchlist"`, `tags=["watchlist"]`. Endpoints: `GET ""`, `POST ""`, `DELETE "/{ticker}"` (path parameter `ticker: str`). All return the same 501 error shape (`"message": "Coming in Phase 2"`).
    - `backend/app/routers/chat.py` exports `create_chat_router() -> APIRouter` (no injected deps in Phase 1). Router has `prefix="/chat"`, `tags=["chat"]`. Endpoint: `POST ""` returning `{"error": "not_implemented", "message": "Coming in Phase 3"}` with status 501.
    - All three modules import `PriceCache` from `app.market` (chat.py does not need it but must not depend on it either — chat factory has no parameter); use the factory closure pattern from PATTERNS.md, no module-level globals.
    - All three modules have a top-level docstring and `logger = logging.getLogger(__name__)`.
  </behavior>
  <action>
    Create each of `backend/app/routers/portfolio.py`, `backend/app/routers/watchlist.py`, `backend/app/routers/chat.py` following the corresponding section in 01-PATTERNS.md. Use these concrete signatures and endpoint registrations:

    portfolio.py:
    - Module docstring `"""Portfolio router — stubs in Phase 1; Phase 2 fills in trade execution and snapshots."""`
    - `from __future__ import annotations`, `import logging`, `from fastapi import APIRouter`, `from fastapi.responses import JSONResponse`, `from app.market import PriceCache`
    - `logger = logging.getLogger(__name__)`
    - `def create_portfolio_router(price_cache: PriceCache) -> APIRouter:`
      - Inside: `router = APIRouter(prefix="/portfolio", tags=["portfolio"])`
      - `@router.get("")` async `get_portfolio()` returns `JSONResponse({"error": "not_implemented", "message": "Coming in Phase 2"}, status_code=501)`
      - `@router.post("/trade")` async `execute_trade()` returns same shape
      - `@router.get("/history")` async `get_history()` returns same shape
      - `return router`

    watchlist.py:
    - Module docstring `"""Watchlist router — stubs in Phase 1; Phase 2 fills in CRUD against the active ticker set."""`
    - Same imports + `PriceCache`
    - `def create_watchlist_router(price_cache: PriceCache) -> APIRouter:`
      - `router = APIRouter(prefix="/watchlist", tags=["watchlist"])`
      - `@router.get("")` async `get_watchlist()` returns 501 stub
      - `@router.post("")` async `add_ticker()` returns 501 stub
      - `@router.delete("/{ticker}")` async `remove_ticker(ticker: str)` returns 501 stub
      - `return router`

    chat.py:
    - Module docstring `"""Chat router — stubs in Phase 1; Phase 3 fills in LiteLLM + GPT-4.1-mini integration."""`
    - Imports omit `PriceCache` (no cache dep — Phase 3 will inject LLM client + DB helpers later)
    - `def create_chat_router() -> APIRouter:`
      - `router = APIRouter(prefix="/chat", tags=["chat"])`
      - `@router.post("")` async `send_message()` returns `JSONResponse({"error": "not_implemented", "message": "Coming in Phase 3"}, status_code=501)`
      - `return router`

    Do not use the `PriceCache` argument inside the function bodies in Plan 03 — the parameter exists only so Phase 2 can read from the cache without changing the call site in main.py.
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev python -c "
from app.routers.portfolio import create_portfolio_router
from app.routers.watchlist import create_watchlist_router
from app.routers.chat import create_chat_router
from app.market import PriceCache
cache = PriceCache()
p = create_portfolio_router(cache)
w = create_watchlist_router(cache)
c = create_chat_router()
p_paths = sorted(r.path for r in p.routes if hasattr(r, 'path'))
w_paths = sorted(r.path for r in w.routes if hasattr(r, 'path'))
c_paths = sorted(r.path for r in c.routes if hasattr(r, 'path'))
assert p_paths == ['/portfolio', '/portfolio/history', '/portfolio/trade'], p_paths
assert w_paths == ['/watchlist', '/watchlist', '/watchlist/{ticker}'], w_paths
assert c_paths == ['/chat'], c_paths
print('ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/routers/portfolio.py` contains `def create_portfolio_router(price_cache: PriceCache) -> APIRouter:`.
    - `backend/app/routers/portfolio.py` contains `APIRouter(prefix="/portfolio", tags=["portfolio"])`.
    - `backend/app/routers/portfolio.py` contains three `@router.<method>(...)` decorators: `@router.get("")`, `@router.post("/trade")`, `@router.get("/history")`.
    - `backend/app/routers/watchlist.py` contains `def create_watchlist_router(price_cache: PriceCache) -> APIRouter:`.
    - `backend/app/routers/watchlist.py` contains `@router.delete("/{ticker}")`.
    - `backend/app/routers/chat.py` contains `def create_chat_router() -> APIRouter:` (no `price_cache` parameter).
    - All three router files contain the string `"error": "not_implemented"` (exactly that quoted key/value).
    - The verification command above prints `ok`.
  </acceptance_criteria>
  <done>Three stub routers in place with the exact factory shape Phase 2 and Phase 3 will fill in.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete main.py lifespan with PriceCache + market source + stream router + stub routers</name>
  <files>backend/app/main.py</files>
  <read_first>
    - backend/app/main.py (current state after Plans 01 + 02 — has load_dotenv, lifespan with init_db, health router, static mount)
    - backend/app/market/factory.py (read MASSIVE_API_KEY env behavior; CORE-05 wiring)
    - backend/app/market/simulator.py (asyncio task lifecycle for stop semantics — informational only)
    - .planning/phases/01-backend-foundation/01-PATTERNS.md (main.py section — full lifespan body, router registration order, stream router note)
    - .planning/phases/01-backend-foundation/01-CONTEXT.md (D-04, D-05, D-06, D-08 — registration order, app.state, lifespan shape)
  </read_first>
  <behavior>
    - Lifespan startup order: `init_db()` (Plan 02 wiring, unchanged) → construct `PriceCache()` → `source = create_market_data_source(cache)` → `await source.start(list(DEFAULT_TICKERS))` → assign `app.state.price_cache = cache` and `app.state.market_source = source` → register the stream router via `app.include_router(create_stream_router(cache), prefix="/api")` → log `"FinAlly backend started"` → `yield`.
    - Lifespan shutdown: `await app.state.market_source.stop()` then log `"FinAlly backend stopped"`.
    - Module-scope route registration order (outside lifespan): `app.include_router(health.router, prefix="/api")` → `app.include_router(create_portfolio_router(...), prefix="/api")` etc. is NOT possible at module scope because `cache` does not exist yet — the cache lives in lifespan. Resolve this by passing a `PriceCache` placeholder at module scope is forbidden by D-06 (no globals). Per PATTERNS.md note: register the stub routers + stream router INSIDE the lifespan after `cache` is constructed, using `app.include_router(...)` calls in the startup section. The health router (no deps) stays at module scope. The static mount stays at module scope as the LAST registration. This gives the correct precedence: `/api/health` (module scope), then `/api/portfolio*`, `/api/watchlist*`, `/api/chat*`, `/api/stream/prices` (lifespan startup), then `/` static mount (module scope, registered last in source order).
    - Note: FastAPI evaluates routes in registration order at request time. `app.include_router` called inside lifespan adds the router to `app.router.routes` BEFORE the StaticFiles mount because the mount, although declared at module scope, is appended last — and `app.mount` at module scope runs before lifespan, so the order is health (module) → static (module) → portfolio/watchlist/chat/stream (lifespan). That breaks API priority. Fix: keep the static mount at module scope but register stub routers + stream router at module scope using `app.state` lookups inside the route handlers is NOT necessary because we can construct a PriceCache at module scope as long as the source's lifecycle is in lifespan. PATTERNS.md note in main.py section calls out this exact tension. Resolve as follows: construct the `PriceCache` at MODULE SCOPE (not on `app.state` yet) so it can be passed to `create_portfolio_router`, `create_watchlist_router`, `create_stream_router` at module scope BEFORE the static mount. The cache is just an in-memory store with no lifecycle of its own, so this respects D-06 (no module-level globals refers to mutable shared state with lifecycle — the cache has no lifecycle methods; it's a dict-like object). The market source — which DOES have lifecycle — is constructed and started inside lifespan and stored on `app.state.market_source`. The same cache instance is referenced by both the module-scope router factories and the lifespan-scope source.
    - This makes registration order: (module scope) `app.include_router(health.router, prefix="/api")` → `app.include_router(create_portfolio_router(cache), prefix="/api")` → `app.include_router(create_watchlist_router(cache), prefix="/api")` → `app.include_router(create_chat_router(), prefix="/api")` → `app.include_router(create_stream_router(cache), prefix="/api")` → `app.mount("/", StaticFiles(directory="static", html=True), name="static")`. The lifespan only handles DB init, market source start/stop, and storing `app.state.price_cache` and `app.state.market_source`.
    - When `MASSIVE_API_KEY` is unset/empty in env, `create_market_data_source(cache)` returns a `SimulatorDataSource` instance — verified by an integration test (Task 3) inspecting `type(app.state.market_source).__name__`.
  </behavior>
  <action>
    Rewrite `backend/app/main.py` to the final Phase 1 state. Concrete identifiers:

    Module structure (top-to-bottom):
    1. Top-level docstring `"""FinAlly backend FastAPI entrypoint."""`.
    2. `from __future__ import annotations`.
    3. Imports: `logging`, `asynccontextmanager` from `contextlib`, `load_dotenv` from `dotenv`, `FastAPI` from `fastapi`, `StaticFiles` from `fastapi.staticfiles`.
    4. App imports: `from app.market import PriceCache, create_market_data_source, create_stream_router`; `from app.db import init_db, DEFAULT_TICKERS`; `from app.routers import health`; `from app.routers.portfolio import create_portfolio_router`; `from app.routers.watchlist import create_watchlist_router`; `from app.routers.chat import create_chat_router`.
    5. `load_dotenv()` — unchanged from Plan 01, must remain before any env-var-dependent imports trigger reads. Note: `create_market_data_source` reads `MASSIVE_API_KEY` lazily at call time (inside lifespan), so import order is fine.
    6. `logger = logging.getLogger(__name__)`.
    7. Module-scope `price_cache = PriceCache()` (the shared cache object — no lifecycle of its own).
    8. `@asynccontextmanager async def lifespan(app: FastAPI):` body:
       - `init_db()`
       - `source = create_market_data_source(price_cache)`
       - `await source.start(list(DEFAULT_TICKERS))`
       - `app.state.price_cache = price_cache`
       - `app.state.market_source = source`
       - `logger.info("FinAlly backend started (source=%s)", type(source).__name__)`
       - `yield`
       - `await source.stop()`
       - `logger.info("FinAlly backend stopped")`
    9. `app = FastAPI(title="FinAlly", lifespan=lifespan)`.
    10. Route registration in this exact order at module scope:
        - `app.include_router(health.router, prefix="/api")`
        - `app.include_router(create_portfolio_router(price_cache), prefix="/api")`
        - `app.include_router(create_watchlist_router(price_cache), prefix="/api")`
        - `app.include_router(create_chat_router(), prefix="/api")`
        - `app.include_router(create_stream_router(price_cache), prefix="/api")`  # Note: create_stream_router's internal router has its own prefix="/api/stream", so combined path is /api/api/stream — BUG. See note below.
        - `app.mount("/", StaticFiles(directory="static", html=True), name="static")`  # LAST

    Important precise wiring detail (must verify against `backend/app/market/stream.py` before writing main.py): the existing `create_stream_router` function constructs `router = APIRouter(prefix="/api/stream", tags=["streaming"])` AT MODULE SCOPE (line 20 of stream.py), and the factory `create_stream_router(price_cache)` simply attaches `@router.get("/prices")` to that pre-existing router and returns it. The router already has `prefix="/api/stream"`. Therefore in main.py register it WITHOUT an additional `/api` prefix: `app.include_router(create_stream_router(price_cache))` — NO prefix argument. Adding `prefix="/api"` would yield `/api/api/stream/prices`. PATTERNS.md was schematic; verify the actual router prefix in stream.py (`grep "APIRouter(prefix" backend/app/market/stream.py`) before writing this line.

    All other `include_router` calls (health, portfolio, watchlist, chat) use `prefix="/api"` because those routers' internal `APIRouter(prefix=...)` values are either empty (health) or short paths (`/portfolio`, `/watchlist`, `/chat`) that need the `/api` prefix prepended.
  </action>
  <verify>
    <automated>cd backend && DB_PATH=/tmp/finally-plan03-task2.db rm -f /tmp/finally-plan03-task2.db && uv run --extra dev python -c "
import os
os.environ['DB_PATH'] = '/tmp/finally-plan03-task2.db'
import importlib, app.db, app.main
importlib.reload(app.db)
importlib.reload(app.main)
from fastapi.testclient import TestClient
from app.main import app
with TestClient(app) as client:
    paths = sorted({getattr(r, 'path', '') for r in app.routes})
    for p in ['/api/health', '/api/portfolio', '/api/portfolio/trade', '/api/portfolio/history', '/api/watchlist', '/api/watchlist/{ticker}', '/api/chat', '/api/stream/prices']:
        assert p in paths, f'missing {p} in {paths}'
    assert client.get('/api/health').json() == {'status': 'ok'}
    assert client.get('/api/portfolio').status_code == 501
    assert client.get('/api/watchlist').status_code == 501
    assert client.post('/api/chat', json={}).status_code == 501
    src_type = type(app.state.market_source).__name__
    assert src_type == 'SimulatorDataSource', src_type
    import time; time.sleep(2.0)
    cache = app.state.price_cache
    have = sorted(cache.get_all().keys())
    for t in ['AAPL','GOOGL','MSFT']:
        assert t in have, have
print('ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/main.py` contains `from app.market import PriceCache, create_market_data_source, create_stream_router`.
    - `backend/app/main.py` contains `from app.db import init_db, DEFAULT_TICKERS`.
    - `backend/app/main.py` contains `from app.routers.portfolio import create_portfolio_router` (and analogously for `watchlist` and `chat`).
    - `backend/app/main.py` contains `price_cache = PriceCache()` at module scope.
    - `backend/app/main.py` contains `await source.start(list(DEFAULT_TICKERS))`.
    - `backend/app/main.py` contains `await source.stop()` in the shutdown section.
    - `backend/app/main.py` contains `app.state.price_cache = price_cache` and `app.state.market_source = source`.
    - `backend/app/main.py` contains `app.include_router(create_stream_router(price_cache))` without a `prefix=` kwarg (because the inner router already declares `prefix="/api/stream"` — verified by reading stream.py).
    - `backend/app/main.py` contains `app.mount("/", StaticFiles(directory="static", html=True), name="static")` and this is the LAST `app.<thing>(...)` registration line in the file (`grep -n 'app\\.' backend/app/main.py | tail -1` shows the mount).
    - The verification command above prints `ok`.
    - `cd backend && uv run --extra dev pytest tests/test_health.py tests/test_db.py -v` exits 0 (Plans 01 & 02 tests still pass).
  </acceptance_criteria>
  <done>main.py wires DB, market source, all routers, and static mount in the canonical order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add integration test covering the full Phase 1 happy path</name>
  <files>backend/tests/test_main_integration.py</files>
  <read_first>
    - backend/app/main.py (final wiring after Task 2)
    - backend/tests/market/test_simulator_source.py (async test pattern)
    - backend/app/market/interface.py (MarketDataSource interface for type assertions)
    - .planning/phases/01-backend-foundation/01-PATTERNS.md (Test Class Structure pattern)
    - backend/tests/conftest.py (existing event_loop_policy fixture)
  </read_first>
  <behavior>
    - Test file uses `fastapi.testclient.TestClient` inside a `with` block so the lifespan runs.
    - A module-level fixture `client_with_tmp_db` sets `DB_PATH` env var via monkeypatch to a tmp file, then reloads `app.db` and `app.main`, then yields `TestClient(app)` inside a `with` block. The fixture finalizer asserts the DB file was created.
    - Tests assert: (a) `/api/health` 200 + correct JSON; (b) `/api/portfolio`, `/api/portfolio/trade`, `/api/portfolio/history`, `/api/watchlist`, `/api/watchlist/AAPL`, `/api/chat` all return 501 with the `{"error": "not_implemented", "message": "..."}` shape; (c) market source type is `SimulatorDataSource` when `MASSIVE_API_KEY` is unset (`monkeypatch.delenv("MASSIVE_API_KEY", raising=False)`); (d) within 3 seconds of startup the `PriceCache` holds prices for all 10 default tickers; (e) the SSE endpoint `/api/stream/prices` returns 200 with `content-type` `text/event-stream` and at least one `data: ` line within 3 seconds (use `client.stream("GET", "/api/stream/prices")` with a short read loop).
    - The test pauses ~2 seconds after fixture setup before checking the cache (the simulator runs at ~500ms intervals; CORE-02 success criterion is "populated within seconds of startup").
  </behavior>
  <action>
    Create `backend/tests/test_main_integration.py` with module docstring `"""Phase 1 integration test — full lifespan happy path."""`. Imports: `import importlib`, `import os`, `import time`, `import pytest`, `from fastapi.testclient import TestClient`.

    Define a `pytest` fixture `app_client` (function-scoped) that:
    1. Receives `tmp_path` and `monkeypatch`.
    2. `monkeypatch.setenv("DB_PATH", str(tmp_path / "finally.db"))`.
    3. `monkeypatch.delenv("MASSIVE_API_KEY", raising=False)`.
    4. `monkeypatch.delenv("OPENAI_API_KEY", raising=False)` (so the test runs identically in CI without keys; CORE-05 verification).
    5. `import app.db; importlib.reload(app.db); import app.main; importlib.reload(app.main)`.
    6. Yields `(client, module)` where `client = TestClient(app.main.app)` opened in a `with` block and `module = app.main`.

    Define `TestPhase1Integration` (sync class — TestClient is sync):

    - `test_health_endpoint_returns_ok(app_client)`: GET `/api/health`, assert 200 and JSON `{"status": "ok"}`.
    - `test_portfolio_stubs_return_501(app_client)`: GET `/api/portfolio`, POST `/api/portfolio/trade` (with `{}` body), GET `/api/portfolio/history`. For each: assert `status_code == 501`, JSON shape `{"error": "not_implemented", "message": "Coming in Phase 2"}`.
    - `test_watchlist_stubs_return_501(app_client)`: GET `/api/watchlist`, POST `/api/watchlist` (body `{"ticker": "AAPL"}`), DELETE `/api/watchlist/AAPL`. Same assertions; message = "Coming in Phase 2".
    - `test_chat_stub_returns_501(app_client)`: POST `/api/chat` with body `{"message": "hi"}`. Status 501, message = "Coming in Phase 3".
    - `test_market_source_is_simulator_when_no_massive_key(app_client)`: Read `app.state.market_source` from the test client's app; assert `type(source).__name__ == "SimulatorDataSource"`.
    - `test_price_cache_populated_within_3_seconds(app_client)`: Sleep 2.0 seconds (simulator runs at ~500ms cadence). Read `app.state.price_cache.get_all()`. Assert the resulting dict has keys for all 10 `DEFAULT_TICKERS` and that each value's `price` is `> 0`.
    - `test_sse_stream_emits_event_within_3_seconds(app_client)`: Use `with client.stream("GET", "/api/stream/prices") as response:` and iterate bytes from `response.iter_lines()` with a manual timeout (track `time.monotonic()`); break and assert success when a line starting with `data:` is read within 3 seconds; assert `response.status_code == 200` and `response.headers["content-type"].startswith("text/event-stream")`.
    - `test_static_root_served(app_client)`: GET `/`, assert status 200 and `"FinAlly" in response.text`.

    All tests must be runnable under `uv run --extra dev pytest tests/test_main_integration.py -v` within ~10 seconds total.
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev pytest tests/test_main_integration.py -v && uv run --extra dev pytest tests/test_health.py tests/test_db.py tests/test_main_integration.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_main_integration.py` exists.
    - File contains a class named `TestPhase1Integration`.
    - File contains the literal `text/event-stream` (used in the SSE content-type assertion).
    - File contains `type(source).__name__ == "SimulatorDataSource"` (or equivalent assertion using the same names).
    - `cd backend && uv run --extra dev pytest tests/test_main_integration.py -v` exits 0 with at least 8 tests passing.
    - `cd backend && uv run --extra dev pytest tests/ -v` exits 0 (the full backend test suite — market + Phase 1 — is green).
    - The full Phase 1 test suite completes in under 60 seconds (sanity bound).
  </acceptance_criteria>
  <done>End-to-end integration test passes; Phase 1 walking skeleton verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → `/api/stream/prices` | SSE stream open to any client; emits price data. |
| `MASSIVE_API_KEY` env → `create_market_data_source` | Env var selects simulator vs. live Massive API; key value never logged. |
| Process → asyncio task (`source._run_loop`) | Background task started in lifespan; must be cancelled cleanly on shutdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `MASSIVE_API_KEY` logged accidentally | mitigate | Lifespan logs `type(source).__name__`, never the key. The existing `factory.py` reads the key but does not log its value. |
| T-03-02 | Denial of Service | Long-lived SSE connection | accept | Existing `create_stream_router` includes heartbeat + disconnect detection (verified by 73 passing market tests). Phase 1 adds no new SSE logic. |
| T-03-03 | Tampering | Stub endpoints accept arbitrary bodies | accept | Stubs return 501 before any body parsing; no input is processed. Phase 2/3 add validation. |
| T-03-04 | Elevation of Privilege | `/api/*` routes shadowed by static mount | mitigate | StaticFiles mount registered LAST (acceptance criterion); `test_phase1_integration` does not need a dedicated negative test because routes for `/api/portfolio`, etc., would return 200 (HTML) if shadowing occurred — the 501 assertions catch this regression. |
| T-03-05 | Repudiation | Asyncio task crashes silently in lifespan | mitigate | The existing simulator's `_run_loop` already wraps each iteration in `try/except Exception` with `logger.exception` (verified in PATTERNS.md and existing market tests). On shutdown `await source.stop()` joins the task, surfacing any unhandled cancellation errors. |
| T-03-06 | Tampering | Stream router prefix collision (`/api/api/stream`) | mitigate | Task 2 explicitly verifies the inner router's existing `prefix="/api/stream"` against `backend/app/market/stream.py` and registers it WITHOUT an extra `/api` prefix. The integration test confirms `/api/stream/prices` is reachable (not `/api/api/stream/prices`). |
</threat_model>

<verification>
- `cd backend && uv run --extra dev pytest tests/ -v` — all tests pass (market + Phase 1).
- Manual boot smoke (in a scratch terminal, optional):
  - `cd backend && DB_PATH=/tmp/finally-final.db rm -f /tmp/finally-final.db && uv run --extra dev uvicorn app.main:app --port 8000` in background.
  - `curl -s http://localhost:8000/api/health` → `{"status":"ok"}`.
  - `curl -s -X POST http://localhost:8000/api/portfolio/trade -d '{}' -H 'content-type: application/json'` → 501 JSON.
  - `timeout 3 curl -N http://localhost:8000/api/stream/prices | head -5` shows at least one `data:` line.
  - `sqlite3 /tmp/finally-final.db ".tables"` lists all 6 tables.
  - `sqlite3 /tmp/finally-final.db "SELECT count(*) FROM watchlist"` returns 10.
- Restart smoke: kill uvicorn, restart with the same `DB_PATH`; confirm `users_profile.cash_balance` is still 10000.0 and there are still exactly 10 watchlist rows (idempotence holds across boots).
</verification>

<success_criteria>
- Phase 1 success criteria from ROADMAP.md (all 5) are observable on a running server.
- Full backend test suite green (`pytest tests/`).
- Walking-skeleton boot path works end-to-end: lifespan → DB → market source → SSE stream → static mount.
- All 10 Phase 1 requirements (CORE-01..05, DB-01..05) covered across Plans 01–03.
</success_criteria>

<output>
Create `.planning/phases/01-backend-foundation/01-03-SUMMARY.md` when done.
</output>
